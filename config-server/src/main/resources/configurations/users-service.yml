server:
  port: 8131

spring:
  application:
    name: users-service
  datasource:
    url: jdbc:mysql://localhost:3306/alicatados_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

# Configuración JWT
jwt:
  secret: AlicatadosPlasenciaSecretKey2025MicroservicesSystemVerySecure
  expiration: 86400000

logging:
  level:
    com.alicatadosplasencia: DEBUG