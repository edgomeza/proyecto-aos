spring:
  application:
    name: accounting-service
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
  cloud:
    config:
      enabled: true
      fail-fast: false

server:
  port: 8121